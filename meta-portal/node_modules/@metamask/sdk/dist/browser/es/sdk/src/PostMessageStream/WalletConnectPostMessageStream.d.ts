/// <reference types="node" />
/// <reference types="node" />
import { Duplex } from 'stream';
import { PostMessageStream } from './PostMessageStream';
export declare class WalletConnectPostMessageStream extends Duplex implements PostMessageStream {
    private _alreadySubscribed;
    private _name;
    wcConnector: any;
    constructor({ name, wcConnector }: {
        name: string;
        wcConnector: any;
    });
    _write(chunk: any, _encoding: BufferEncoding, _cb: (error?: Error | null) => void): void;
    _read(): undefined;
    _onMessage(event: any): void;
    setProviderState({ chainId, accounts, }: {
        chainId: string;
        accounts: string[];
    }): void;
    subscribeToConnectionEvents(): void;
    start(): void;
}
//# sourceMappingURL=WalletConnectPostMessageStream.d.ts.map